Para desarrolladores, usuarios de git y demás. se realizó cambio en la librería del juego, el climb provider, FinishLocomotion() accesible a traves de packages/xr interaction toolkit/runtime/locomotion/climbing. (o simplemente dentro de los locomotion providers del xr origin) necesita ser publico para que el custom climb funcione. Sin esto, el editor no carga los monobehavior y no se puede manipular bien la escena. 